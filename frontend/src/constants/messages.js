import { defineMessages } from 'react-intl';

const messages = defineMessages({
    sidebarUploadDocument: {
        id: 'sidebar.uploadDocument',
        defaultMessage: 'Upload Document',
    },
});

export default messages;
